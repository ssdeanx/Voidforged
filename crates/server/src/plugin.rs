use bevy::prelude::*;

pub struct ServerPlugin;

impl Plugin for ServerPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: Add server-specific systems (player management, room instances)
    }
}
