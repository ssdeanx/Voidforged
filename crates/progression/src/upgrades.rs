use bevy::prelude::*;

/// Placeholder for permanent upgrade system (unlocks, stat upgrades, weapon evolutions).
pub struct UpgradesPlugin;

impl Plugin for UpgradesPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: Implement meta-progression UI and upgrade tree
    }
}
